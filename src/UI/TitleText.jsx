import { Text } from "@chakra-ui/react";
const TitleText = ({ children }) => {
  return <Text fontWeight={'bold'} fontFamily={'space'} my={'1'} color={'yellow'}>{children}</Text>;
};
export default TitleText
